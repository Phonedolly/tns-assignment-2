import numpy as np
import quantumrandom as qt


# 정규분포를 따르는 샘플들의 배열을 생성한다
def get_sample(num_experiments, num_samples, mean, variance):
    sample = np.array((num_experiments, num_samples))

    return sample
